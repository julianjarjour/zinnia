[user]
    email = tonijarjour@posteo.com
    name = Toni Jarjour

[alias]
    a = add
    b = branch
    c = commit
    d = diff
    ds = diff --staged
    dn = diff --no-index
    h = stash
    i = init
    k = checkout
    l = pull
    m = merge
    n = clone --depth 1
    o = log --oneline -n 20 --graph
    p = push
    r = reset
    s = status -s
    t = remote
    v = revert
    w = show

